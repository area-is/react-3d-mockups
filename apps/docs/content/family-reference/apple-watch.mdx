`<AppleWatch>` and `<AppleWatchMockup>` render an Apple Watch - the Series 11,
the Series 12 or the Ultra 4, chosen with `variant` - wearing a real wristband
on an invisible wrist directly behind the case: the band sliding into the
slots machined in the case edges, exactly how worn-band product photos pose
it.

The Series case is a 46 mm squircle with the gear-toothed knurled Digital
Crown, the flush side button in its machined recess, a drilled mic hole and a
machined speaker slot on the left edge, under an edge-to-edge crystal over the
live 208×248 rounded-rect face. The Ultra 4 is the 49 mm titanium case: flat
sides and tighter corners, a flat crystal over a 211×257 face, a raised crown
guard on the right flank shielding a larger crown and the side button, and the
orange Action button on the left between two speaker slots - orange whatever
the case finish, because on the hardware it is. Every back carries its real
sensor cluster: an optical heart sensor behind a round crystal ringed by the
ECG electrode, sunk flush into a body-colour plate with the charging-coil ring
engraved outside it.

The Series wear the **Solo Loop**: one continuous stretchy band, sized to the
wrist rather than adjusted, so there is no closure, no adjustment holes and no
hardware anywhere on it - it just flares into the lug slots at both ends. The
Ultra wears its Ocean Band, a buckled two-strap band at the Ultra's wider strap
width. Neither unfastens here, which is why this component has no `bandOpen`
prop. For a band that lays open, see [Galaxy Watch](/docs/api/galaxy-watch-8).

<MockupExplorer
  component="AppleWatchMockup"
  props={{ float: true, color: '#1c1d21', bandColor: '#33415c', rotation: [0, -0.35, 0] }}
/>

## `<AppleWatch>` vs `<AppleWatchMockup>`

<BareVsMockup object="AppleWatch" kind="appleWatch" slot="Screen" />

```tsx
import { MockupCanvas, AppleWatch, AppleWatchMockup } from 'react-3d-mockups'

// The one-liner: its own canvas, with lighting, a ground shadow, drag-to-orbit
// controls and a camera already framed on the object.
<AppleWatchMockup float style={{ height: 520 }}>
  <YourApp />
</AppleWatchMockup>

// The same model with no stage of its own. Inside a canvas you arrange
// yourself it can be posed, animated, lit and joined by anything else.
<MockupCanvas>
  <AppleWatch rotation={[0, 0.4, 0]}>
    <YourApp />
  </AppleWatch>
</MockupCanvas>
```

## Props

Beyond the [shared screen props](/docs/screen-content):

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `variant` | `'series11' \| 'series12' \| 'ultra4'` | `'series11'` | Which Apple Watch to render. The Series 12 is the Series 11's case a millimetre wider, with the generation's finishes; the Ultra 4 is the 49 mm titanium case with the crown guard and the Action button. |
| `color` | `string` | `'#1c1d21'` | Case. Takes a retail colorway id from `APPLE_WATCH_COLORWAYS[variant]` (Series 11 `jetblack`, `spacegray`, `rosegold`, `silver`; Series 12 `darkbronze`, `lightgold`, `black`, `spacegray`, `radiantgold`, `natural`, `pearlwhite`, `nightblue`; Ultra 4 `natural`, `black`) or any CSS color. |
| `bandColor` | `string` | `'#2a2c31'` | Band colorway - the Solo Loop on the Series, the Ocean Band on the Ultra. |
| `resolution` | `number` | per variant | Virtual display width - each model's own point grid (208 on the Series, 211 on the Ultra), so watchOS-sized layouts apply. |

There is no `orientation` prop, and no `bandOpen`.

## The models

### Apple Watch Series 11

The 46 mm aluminium squircle with the 1.96″ 416×496 panel (208×248 logical).
Retail finishes: `jetblack`, `spacegray`, `rosegold`, `silver`.

<MockupExplorer
  component="AppleWatchMockup"
  props={{ variant: 'series11', color: 'jetblack', bandColor: '#33415c', rotation: [0, -0.35, 0] }}
/>

### Apple Watch Series 12

The Series 11's case a millimetre wider (Apple publishes 46 × 40 × 9.7 mm
against the 11's 46 × 39) over the same panel - the generation is the S11
chip, the sensors and a ceramic case option, none of it visible from outside
- so the model carries the Series 11's geometry at that width and the finishes
carry the year: aluminium `darkbronze`, `lightgold`, `black`, `spacegray`;
titanium `radiantgold`, `natural`; ceramic `pearlwhite`, `nightblue`.

<MockupExplorer
  component="AppleWatchMockup"
  props={{ variant: 'series12', color: 'darkbronze', bandColor: '#5b4a3c', rotation: [0, -0.35, 0] }}
/>

### Apple Watch Ultra 4

The 49 mm grade 5 titanium case the Ultra 3 introduced: flat sides, tighter
corners, a flat sapphire crystal over the 1.98″ 422×514 panel (211×257
logical), the crown guard with the orange-ringed crown, the orange Action
button and the buckled Ocean Band. Apple's 44 mm width takes in the guard and
crown; the case itself is 41.4 mm across, per Apple's bezel drawing. Retail finishes: `natural`, `black`.

<MockupExplorer
  component="AppleWatchMockup"
  props={{ variant: 'ultra4', color: 'black', bandColor: '#1f2124', rotation: [0, -0.35, 0] }}
/>

## Mockup-able areas

Every live surface filled with chroma green, labeled with the prop that
feeds it - the fastest way to see exactly where your content lands and
what stays 3D hardware.

<MockupExplorer component="AppleWatchMockup" screen="chroma" />
