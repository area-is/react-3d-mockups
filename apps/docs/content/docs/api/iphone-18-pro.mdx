---
title: iPhone 18 Pro
description: The iPhone 18 Pro in WebGL, with a live prop explorer and the full IPhoneMockup reference.
---

Drive every prop from the inspector; the `demo.tsx` panel rewrites itself to
exactly what is being passed. The explorer is pinned to `variant="18pro"`; everything below applies to the whole family.

<MockupExplorer component="IPhoneMockup" variant="18pro" />

`<IPhone>` and `<IPhoneMockup>` render an iPhone 17 family device or an
iPhone 18 Pro: flat frame, Dynamic Island, Action button, Camera Control, and
per-model rear cameras. The camera architecture follows the real devices:
two-lens pill (17), ultra-thin single-lens bar (Air), full-width triple-lens
plateau with flash and LiDAR (the Pros). The 18 Pro and 18 Pro Max are the
17 Pros' chassis under the generation's narrower Dynamic Island and
colour-matched back. For the foldable, see [iPhone Duo](/docs/api/iphone-duo).

## `<IPhone>` vs `<IPhoneMockup>`

<BareVsMockup object="IPhone" kind="iphone" slot="Screen" />

```tsx
import { MockupCanvas, IPhone, IPhoneMockup } from 'react-3d-mockups'

// The one-liner: its own canvas, with lighting, a ground shadow, drag-to-orbit
// controls and a camera already framed on the object.
<IPhoneMockup float style={{ height: 520 }}>
  <YourApp />
</IPhoneMockup>

// The same model with no stage of its own. Inside a canvas you arrange
// yourself it can be posed, animated, lit and joined by anything else.
<MockupCanvas>
  <IPhone rotation={[0, 0.4, 0]}>
    <YourApp />
  </IPhone>
</MockupCanvas>
```

## Props

Same API as [`Galaxy`](/docs/api/galaxy-s26), except:

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `variant` | `'17' \| 'air' \| 'pro' \| 'promax' \| '18pro' \| '18promax'` | `'17'` | Which iPhone: 17 (two-lens pill), air (ultra-thin, single-lens bar), pro and promax (full-width plateau, flash + LiDAR), 18pro and 18promax (the same two Pro bodies with the narrower Dynamic Island and colour-matched back). |
| `orientation` | `'portrait' \| 'landscape'` | `'portrait'` | Landscape lays the device on its side and swaps the virtual display (for example 874×402 on the 17) with upright content. |
| `statusBar` | `boolean \| StatusBarContent` | `false` | Draw the system status bar across the top of the screen. `true` gives the platform's defaults - iOS's, with the clock centred in the ear left of the Dynamic Island and the meters in the ear right of it; an object sets the clock, the meters and the ink (`{{ time: '14:05', battery: 0.42, batteryPercent: true, color: '#1c1c1e' }}`). It is placed from this device's own camera cutout and logical grid, so it lines up with the hardware on every variant. See [Status bar](/docs/screen-content#status-bar). |
| `resolution` | `number` | per variant | Defaults to the variant's point grid: 402×874 (17, pro, 18pro), 420×912 (air), 440×956 (promax, 18promax). |
| `color` | `string` | `'#1a1c20'` | Back glass. Takes a retail colorway id from `IPHONE_COLORWAYS[variant]` - 17 `black`, `white`, `lavender`, `mistblue`, `sage`; Air `spaceblack`, `cloudwhite`, `lightgold`, `skyblue`; 17 Pro/Pro Max `silver`, `cosmicorange`, `deepblue`; 18 Pro/Pro Max `black`, `silver`, `glacier`, `burgundy` - which brings that model's matching chassis rail with it, or any CSS color - the rail is then derived from it. |

## Colorways

17: Lavender `#cfc4e6`, Mist Blue `#b7c9dd`, Sage `#aebfae`. Air: Sky Blue
`#bfd4e6`, Light Gold `#e6d9c0`. 17 Pro: Cosmic Orange `#c96b34`, Deep Blue
`#2b3a55`, Silver `#dfe0e2`. 18 Pro: Glacier `#c4d8e6`, Burgundy `#6a2b3e`,
Black `#1e1f23`, Silver `#dfe0e2`.

## This model

### iPhone 18 Pro

The 17 Pro's chassis to the tenth of a millimetre - the same 6.3″ panel, the
same forged plateau and lens trio, the same 402×874 grid - under the
generation's Dynamic Island, ~35% narrower now that the infrared camera sits
below the display, and with the Ceramic Shield charging window colour-matched
to the unibody rather than two-tone. Retail finishes: `black`, `silver`,
`glacier`, `burgundy`.

<MockupExplorer
  component="IPhoneMockup"
  variant="18pro"
  props={{ color: 'burgundy', rotation: [0, -0.3, 0] }}
/>

## Mockup-able areas

Every live surface filled with chroma green, labeled with the slot that
feeds it - the fastest way to see exactly where your content lands and
what stays 3D hardware.

<MockupExplorer component="IPhoneMockup" variant="18pro" screen="chroma" />



<DeviceDisclaimer brands="Apple" />
