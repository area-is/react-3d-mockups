---
title: Greeting card
description: A standing A7 card with four live faces - covers and inside spread.
---

`<GreetingCard>` and `<GreetingCardMockup>` render a folded 5"×7" (A7) card
standing like a tent, fold toward the viewer: bare children fill the front
cover, `<GreetingCardMockup.Back>` the other outward face, and
`<GreetingCardMockup.InsideLeft>`/`<GreetingCardMockup.InsideRight>` print
the spread you see from behind. Each face gently shades into the crease, like
paper actually bowing.

<MockupExplorer
  component="GreetingCardMockup"
  props={{ float: true, rotation: [0, -0.82, 0] }}
/>

## `<GreetingCard>` vs `<GreetingCardMockup>`

<BareVsMockup object="GreetingCard" kind="greetingCard" slot="Front" />

```tsx
import { MockupCanvas, GreetingCard, GreetingCardMockup } from 'react-3d-mockups'

// The one-liner: its own canvas, with lighting, a ground shadow, drag-to-orbit
// controls and a camera already framed on the object.
<GreetingCardMockup float style={{ height: 520 }}>
  <YourApp />
</GreetingCardMockup>

// The same model with no stage of its own. Inside a canvas you arrange
// yourself it can be posed, animated, lit and joined by anything else.
<MockupCanvas>
  <GreetingCard rotation={[0, 0.4, 0]}>
    <YourApp />
  </GreetingCard>
</MockupCanvas>
```

## Props

Beyond the [shared screen props](/docs/screen-content):

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `openAngle` | `number` | `65` | Angle between the panels (display sweet spot is 60–75). |
| `color` | `string` | `'#f6f3ec'` | Card stock: edges and unprinted faces. |
| `surfaceBackground` | `string` | `'#ffffff'` | CSS background behind each printed face. |
| `resolution` | `number` | `380` | Virtual panel width in CSS px; height follows the A7 panel. |

## Regions

| Slot | Surface |
| --- | --- |
| `<GreetingCardMockup.Front>` | Front cover (bare children). |
| `<GreetingCardMockup.InsideLeft>` | Inside-left panel of the spread. |
| `<GreetingCardMockup.InsideRight>` | Inside-right panel of the spread. |
| `<GreetingCardMockup.Back>` | Back cover - the left outward face. |

Every slot takes per-surface overrides - `surfaceBackground`,
`resolution`, `surfaceStyle` - over the mockup-level defaults. The
same slots exist on the bare `<GreetingCard>`.

## Mockup-able areas

Every live surface filled with chroma green, labeled with the slot that
feeds it - the fastest way to see exactly where your content lands and
what stays 3D hardware.

<MockupExplorer component="GreetingCardMockup" screen="chroma" />

## More examples

### All four faces, orbiting

<MockupExplorer component="GreetingCardMockup" props={{ autoRotate: 1.4 }} />
