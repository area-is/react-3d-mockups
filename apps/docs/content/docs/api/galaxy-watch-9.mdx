---
title: Galaxy Watch 9
description: The Galaxy Watch 9 in WebGL, with a live prop explorer and the full GalaxyWatchMockup reference.
---

Drive every prop from the inspector; the `demo.tsx` panel rewrites itself to
exactly what is being passed. The explorer is pinned to `variant="watch9"`; everything below applies to the whole family.

<MockupExplorer component="GalaxyWatchMockup" variant="watch9" />

`<GalaxyWatch>` and `<GalaxyWatchMockup>` render a Samsung Galaxy Watch - the
Watch 8, the Watch 9 or the Watch Ultra 2, chosen with `variant` - wearing a
real wristband on an invisible wrist directly behind the case: two domed,
tapering straps meeting at a closure on the underside of the wrist, matching
how worn-band product photos pose it.

Every model is the "cushion" squircle carrying a **fully round** display
raised on its dial puck, so the metal stays visible around the live circular
face. The 44 mm Watch 8 and Watch 9 are aluminium with two raised chamfered
keys straddling the mic hole; the 47 mm Watch Ultra 2 is titanium, deeper,
with a three-key run ending in its orange Quick Button - orange whatever the
case finish, because on the hardware it is. The back carries the BioActive
sensor puck standing proud of the case, an optical heart sensor behind its
round crystal ringed by the ECG electrode.

The band is the Dynamic-Lug-style two-strap band: nearly case-wide at the
connector, tapering around the wrist to a stainless pin buckle whose tongue
rises through one of the adjustment holes punched clean through the long
strap, with a keeper holding the tail down. The lengths come from the retail
fit range rather than being eyeballed; the Ultra 2 wears it at its wider
strap width.

## `<GalaxyWatch>` vs `<GalaxyWatchMockup>`

<BareVsMockup object="GalaxyWatch" kind="galaxyWatch" slot="Screen" />

```tsx
import { MockupCanvas, GalaxyWatch, GalaxyWatchMockup } from 'react-3d-mockups'

// The one-liner: its own canvas, with lighting, a ground shadow, drag-to-orbit
// controls and a camera already framed on the object.
<GalaxyWatchMockup float style={{ height: 520 }}>
  <YourApp />
</GalaxyWatchMockup>

// The same model with no stage of its own. Inside a canvas you arrange
// yourself it can be posed, animated, lit and joined by anything else.
<MockupCanvas>
  <GalaxyWatch rotation={[0, 0.4, 0]}>
    <YourApp />
  </GalaxyWatch>
</MockupCanvas>
```

## Props

Beyond the [shared screen props](/docs/screen-content):

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `variant` | `'watch8' \| 'watch9' \| 'watchultra2'` | `'watch8'` | Which Galaxy Watch to render. The Watch 9 is the Watch 8's case with the generation's internals; the Watch Ultra 2 is the 47 mm titanium squircle. |
| `color` | `string` | `'#1c1d21'` | Case. Takes a retail colorway id from `GALAXY_WATCH_COLORWAYS[variant]` (`graphite`/`silver` on the 44 mm models, `titaniumsilver`/`titaniumgray` on the Ultra 2) or any CSS color. |
| `bandColor` | `string` | `'#2a2c31'` | Strap colorway (fluoroelastomer). |
| `bandOpen` | `boolean` | `false` | Lay the band out unbuckled and flat instead of wearing it. |
| `resolution` | `number` | per variant | Virtual display width - each model's own dp grid (240 on the 44 mm models, 249 on the Ultra 2), and the display is round, so give your face a `border-radius: 50%` root (or design edge-to-edge and let the crystal mask it). |

There is no `orientation` prop.

## The models

### Galaxy Watch 9

The Watch 8's case to the tenth of a millimetre - the generation is chip,
battery and Wear OS 7, none of it visible from outside - so the model shares
the Watch 8's measured geometry deliberately. Retail finishes: `graphite`,
`silver`.

<MockupExplorer
  component="GalaxyWatchMockup"
  variant="watch9"
  props={{ color: 'silver', bandColor: '#c9cbd0', rotation: [0, 0.3, 0] }}
/>

## Mockup-able areas

Every live surface filled with chroma green, labeled with the prop that
feeds it - the fastest way to see exactly where your content lands and
what stays 3D hardware.

<MockupExplorer component="GalaxyWatchMockup" variant="watch9" screen="chroma" />

## More examples

### Unbuckled, showing the sensor back

A worn band is a closed loop, and the far side of a closed loop sits between
the camera and the watch - so it always hides the case back. `bandOpen` runs
both straps straight out from the case instead, so the hole row, the buckle and
the BioActive sensor back all face the viewer, the way a band is photographed
off-wrist. The mockup backs the camera off to frame the full length.

<MockupExplorer
  component="GalaxyWatchMockup"
  variant="watch9"
  props={{ bandOpen: true, color: '#33363c', bandColor: '#23252a', rotation: [0, 3.14, 0] }}
/>

<DeviceDisclaimer brands="Samsung" />
