---
title: Business card
description: A 32 pt business card with live front and back faces and painted edges.
---

`<BusinessCard>` and `<BusinessCardMockup>` render an 89×51 mm (3.5"×2")
business card on premium 32 pt stock: square die-cut corners (the standard
for thick stock), an optional painted edge, and a live 520×298 DOM surface
on the front - and, optionally, the back.

<MockupExplorer
  component="BusinessCardMockup"
  props={{ float: true, rotation: [-0.1, -0.35, 0] }}
/>

## `<BusinessCard>` vs `<BusinessCardMockup>`

<BareVsMockup object="BusinessCard" kind="businessCard" slot="Front" />

```tsx
import { MockupCanvas, BusinessCard, BusinessCardMockup } from 'react-3d-mockups'

// The one-liner: its own canvas, with lighting, a ground shadow, drag-to-orbit
// controls and a camera already framed on the object.
<BusinessCardMockup float style={{ height: 520 }}>
  <YourApp />
</BusinessCardMockup>

// The same model with no stage of its own. Inside a canvas you arrange
// yourself it can be posed, animated, lit and joined by anything else.
<MockupCanvas>
  <BusinessCard rotation={[0, 0.4, 0]}>
    <YourApp />
  </BusinessCard>
</MockupCanvas>
```

## Props

Beyond the [shared screen props](/docs/screen-content):

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `color` | `string` | `'#f7f6f2'` | Stock color: the faces' base and unprinted back. |
| `edgeColor` | `string` | stock color | Painted-edge color - the bright seam that says "32 pt". |
| `surfaceBackground` | `string` | `'#ffffff'` | CSS background behind each face's content. |
| `resolution` | `number` | `520` | Virtual face width in CSS px (~150 dpi); height follows the card aspect. |

## Regions

| Slot | Surface |
| --- | --- |
| `<BusinessCardMockup.Front>` | Front face (bare children). |
| `<BusinessCardMockup.Back>` | Back face - plain stock when omitted. |

Every slot takes per-surface overrides - `surfaceBackground`,
`resolution`, `surfaceStyle` - over the mockup-level defaults. The
same slots exist on the bare `<BusinessCard>`.

## Mockup-able areas

Every live surface filled with chroma green, labeled with the slot that
feeds it - the fastest way to see exactly where your content lands and
what stays 3D hardware.

<MockupExplorer component="BusinessCardMockup" screen="chroma" />

## More examples

### Front and back

Add a `<BusinessCardMockup.Back>` and let it orbit - both faces are live DOM.

<MockupExplorer component="BusinessCardMockup" props={{ autoRotate: 2 }} />

### Showing both sides at once

Two bare `<BusinessCard>` objects in one
[`MockupCanvas`](/docs/api/mockup-canvas) - the classic front-and-back
presentation pair.

<MockupExplorer
  component="BusinessCardMockup"
  props={{ camera: { position: [0, 0.2, 8.6], fov: 40 } }}
  arrangement={{
    canvas: { shadowY: -2.1 },
    items: [
      { surface: 'front', props: { position: [-1.6, 0.85, 0], rotation: [-0.06, 0.25, 0.03] } },
      {
        surface: 'back',
        props: { color: '#191d24', position: [1.6, -0.85, 0.2], rotation: [-0.06, -0.3, -0.03] },
      },
    ],
  }}
/>
