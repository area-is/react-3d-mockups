---
title: Custom panel
description: A flat rectangular sheet at any size you specify in millimeters.
---

`<CustomPanel>` and `<CustomPanelMockup>` render a flat rectangular sheet at
ANY size - foam board, acrylic sign, art print, table card. Pass real
millimeters via `size`; the longest edge auto-normalizes to the stage, so a
100×150 mm card and a 1000×1500 mm board both fill the default camera while
keeping their true aspect ratio and relative thickness. Front and back are
live DOM.

<MockupExplorer
  component="CustomPanelMockup"
  props={{ size: { width: 600, height: 900, thickness: 5 }, rotation: [0, 0.25, 0] }}
/>

## `<CustomPanel>` vs `<CustomPanelMockup>`

<BareVsMockup object="CustomPanel" kind="customPanel" slot="Front" />

```tsx
import { MockupCanvas, CustomPanel, CustomPanelMockup } from 'react-3d-mockups'

// The one-liner: its own canvas, with lighting, a ground shadow, drag-to-orbit
// controls and a camera already framed on the object.
<CustomPanelMockup float style={{ height: 520 }}>
  <YourApp />
</CustomPanelMockup>

// The same model with no stage of its own. Inside a canvas you arrange
// yourself it can be posed, animated, lit and joined by anything else.
<MockupCanvas>
  <CustomPanel rotation={[0, 0.4, 0]}>
    <YourApp />
  </CustomPanel>
</MockupCanvas>
```

## Props

Beyond the [shared screen props](/docs/screen-content):

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `size` | `{ width, height, thickness? }` | required | Panel size in millimeters. `thickness` defaults to 5. |
| `color` | `string` | `'#f2f1ed'` | Stock color: edges, and the back when unprinted. |
| `cornerRadius` | `number` | `2` | Corner rounding in millimeters. |
| `surfaceBackground` | `string` | `'#ffffff'` | CSS background behind each printed face. |
| `resolution` | `number` | `560` | Virtual front face width in CSS px. |

## Regions

| Slot | Surface |
| --- | --- |
| `<CustomPanelMockup.Front>` | Front face (bare children). |
| `<CustomPanelMockup.Back>` | Back face. |

Every slot takes per-surface overrides - `surfaceBackground`,
`resolution`, `surfaceStyle` - over the mockup-level defaults. The
same slots exist on the bare `<CustomPanel>`.

`customPanelScale(size)` gives the world-units-per-mm factor (e.g. to position
several panels in one scene). It lives on the `/core` subpath, next to the rest
of the specs:

```ts
import { customPanelScale } from 'react-3d-mockups/core'
```

## Mockup-able areas

Every live surface filled with chroma green, labeled with the slot that
feeds it - the fastest way to see exactly where your content lands and
what stays 3D hardware.

<MockupExplorer component="CustomPanelMockup" screen="chroma" />

## More examples

### Wide display board

<MockupExplorer
  component="CustomPanelMockup"
  props={{
    size: { width: 2400, height: 800, thickness: 12 },
    color: '#20242b',
    float: true,
    rotation: [0, -0.2, 0],
  }}
/>
