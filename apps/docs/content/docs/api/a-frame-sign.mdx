---
title: A-frame sign
description: A sidewalk sandwich board with two live framed panels.
---

`<AFrameSign>` and `<AFrameSignMockup>` render the classic 600×900 mm
chalkboard-class sidewalk sign: two wood-framed panels hinged at the top,
legs splayed ~15° (measured from street photos), with both faces live DOM -
chalk menus, sale boards, open/closed signs.

<MockupExplorer
  component="AFrameSignMockup"
  props={{ rotation: [0, -0.3, 0] }}
/>

## `<AFrameSign>` vs `<AFrameSignMockup>`

<BareVsMockup object="AFrameSign" kind="aFrameSign" slot="Front" />

```tsx
import { MockupCanvas, AFrameSign, AFrameSignMockup } from 'react-3d-mockups'

// The one-liner: its own canvas, with lighting, a ground shadow, drag-to-orbit
// controls and a camera already framed on the object.
<AFrameSignMockup float style={{ height: 520 }}>
  <YourApp />
</AFrameSignMockup>

// The same model with no stage of its own. Inside a canvas you arrange
// yourself it can be posed, animated, lit and joined by anything else.
<MockupCanvas>
  <AFrameSign rotation={[0, 0.4, 0]}>
    <YourApp />
  </AFrameSign>
</MockupCanvas>
```

## Props

Beyond the [shared screen props](/docs/screen-content):

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `color` | `string` | `'#4a3826'` | Frame color - dark-stained wood by default. |
| `surfaceBackground` | `string` | `'#20241f'` | CSS background behind each panel (chalkboard black). |
| `resolution` | `number` | `420` | Virtual face width in CSS px; height follows the panel. |

## Regions

| Slot | Surface |
| --- | --- |
| `<AFrameSignMockup.Front>` | Front panel (bare children). |
| `<AFrameSignMockup.Back>` | Back panel - the other side of the A. |

Every slot takes per-surface overrides - `surfaceBackground`,
`resolution`, `surfaceStyle` - over the mockup-level defaults. The
same slots exist on the bare `<AFrameSign>`.

## Mockup-able areas

Every live surface filled with chroma green, labeled with the slot that
feeds it - the fastest way to see exactly where your content lands and
what stays 3D hardware.

<MockupExplorer component="AFrameSignMockup" screen="chroma" />

## More examples

### Painted frame, both sides

<MockupExplorer
  component="AFrameSignMockup"
  props={{ color: '#31404f', surfaceBackground: '#1c2733', autoRotate: 1.2 }}
/>
