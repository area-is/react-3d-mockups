---
title: Poster frame
description: An 18x24 gallery frame with a live recessed sheet, glazing sheen, and optional mat.
---

`<PosterFrame>` and `<PosterFrameMockup>` render an 18"×24" poster in a 22 mm
gallery frame: an extruded molding whose rabbet lip overlaps the sheet edge
~5 mm per side like a real frame, the sheet shallowly recessed behind the
front lip, a soft acrylic glazing sheen over the art, an optional 2.5"
gallery matboard, and a kraft dust cover on the back. The visible opening is
a live 540×721 DOM surface.

<MockupExplorer
  component="PosterFrameMockup"
  props={{ rotation: [0, 0.25, 0] }}
/>

## `<PosterFrame>` vs `<PosterFrameMockup>`

<BareVsMockup object="PosterFrame" kind="posterFrame" slot="Poster" />

```tsx
import { MockupCanvas, PosterFrame, PosterFrameMockup } from 'react-3d-mockups'

// The one-liner: its own canvas, with lighting, a ground shadow, drag-to-orbit
// controls and a camera already framed on the object.
<PosterFrameMockup float style={{ height: 520 }}>
  <YourApp />
</PosterFrameMockup>

// The same model with no stage of its own. Inside a canvas you arrange
// yourself it can be posed, animated, lit and joined by anything else.
<MockupCanvas>
  <PosterFrame rotation={[0, 0.4, 0]}>
    <YourApp />
  </PosterFrame>
</MockupCanvas>
```

## Props

Beyond the [shared screen props](/docs/screen-content):

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `size` | `{ width?, height? }` | `457×610` | Physical sheet in mm - e.g. `{ width: 610, height: 914 }` for 24"×36", `{ width: 594, height: 841 }` for A1. |
| `color` | `string` | `'#22262e'` | Frame molding color. |
| `mat` | `boolean \| string` | `false` | Mount the art behind a 2.5" gallery matboard. `true` cuts it from the default `#f6f3ec` museum board; a CSS color cuts it from that stock instead, so `mat="#1d1f24"` is a dark mount. |
| `glazing` | `boolean` | `true` | Soft acrylic sheen over the art. |
| `surfaceBackground` | `string` | `'#ffffff'` | CSS background behind your poster content. |
| `resolution` | `number` | `540` | Virtual art width in CSS px; height follows its aspect. |

## Regions

One region: the visible sheet (bare children). Use the explicit
`<PosterFrameMockup.Poster>` slot when you want per-surface overrides -
`surfaceBackground`, `resolution`, `surfaceStyle`.

## Mockup-able areas

Every live surface filled with chroma green, labeled with the prop that
feeds it - the fastest way to see exactly where your content lands and
what stays 3D hardware.

<MockupExplorer component="PosterFrameMockup" screen="chroma" />

## More examples

### Oak molding

`color` takes any CSS color - wood tones work as well as gallery black.

<MockupExplorer
  component="PosterFrameMockup"
  props={{ color: '#b98d5f', surfaceBackground: '#f7f2e8', float: true, rotation: [0, -0.25, 0] }}
/>

### A gallery wall pair

Bare `<PosterFrame>` objects composed in one
[`MockupCanvas`](/docs/api/mockup-canvas).

<MockupExplorer
  component="PosterFrameMockup"
  props={{ camera: { position: [0, 0.3, 10.4], fov: 40 } }}
  arrangement={{
    canvas: { shadowY: -2.5 },
    items: [
      { props: { position: [-2.15, 0, -0.3], rotation: [0, 0.35, 0] } },
      { props: { color: '#e8e5dd', position: [2.15, 0, 0], rotation: [0, -0.25, 0] } },
    ],
  }}
/>
