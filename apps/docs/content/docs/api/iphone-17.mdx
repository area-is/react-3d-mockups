---
title: iPhone 17
description: The iPhone 17 in WebGL, with a live prop explorer and the full IPhoneMockup reference.
---

Drive every prop from the inspector; the `demo.tsx` panel rewrites itself to
exactly what is being passed. The explorer is pinned to `variant="17"`; everything below applies to the whole family.

<MockupExplorer component="IPhoneMockup" variant="17" />

`<IPhone>` and `<IPhoneMockup>` render an iPhone 17 family device: flat frame,
Dynamic Island, Action button, Camera Control, and per-model rear cameras. The
camera architecture follows the real devices: two-lens pill (17), ultra-thin
single-lens bar (Air), full-width triple-lens plateau with flash and LiDAR
(Pro and Pro Max).

## `<IPhone>` vs `<IPhoneMockup>`

<BareVsMockup object="IPhone" kind="iphone" slot="Screen" />

```tsx
import { MockupCanvas, IPhone, IPhoneMockup } from 'react-3d-mockups'

// The one-liner: its own canvas, with lighting, a ground shadow, drag-to-orbit
// controls and a camera already framed on the object.
<IPhoneMockup float style={{ height: 520 }}>
  <YourApp />
</IPhoneMockup>

// The same model with no stage of its own. Inside a canvas you arrange
// yourself it can be posed, animated, lit and joined by anything else.
<MockupCanvas>
  <IPhone rotation={[0, 0.4, 0]}>
    <YourApp />
  </IPhone>
</MockupCanvas>
```

## Props

Same API as [`Galaxy`](/docs/api/galaxy-s26), except:

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `variant` | `'17' \| 'air' \| 'pro' \| 'promax'` | `'17'` | Which iPhone 17 family device: 17 (two-lens pill), air (ultra-thin, single-lens bar), pro and promax (full-width plateau, flash + LiDAR). |
| `orientation` | `'portrait' \| 'landscape'` | `'portrait'` | Landscape lays the device on its side and swaps the virtual display (for example 874×402 on the 17) with upright content. |
| `resolution` | `number` | per variant | Defaults to the variant's point grid: 402×874 (17, pro), 420×912 (air), 440×956 (promax). |
| `color` | `string` | `'#1a1c20'` | Back glass. Takes a retail colorway id from `IPHONE_COLORWAYS` - 17 `black`, `white`, `lavender`, `mistblue`, `sage`; Air `spaceblack`, `cloudwhite`, `lightgold`, `skyblue`; Pro/Pro Max `silver`, `cosmicorange`, `deepblue` - which brings that model's measured chassis rail with it, or any CSS color - the rail is then derived from it. |

## Colorways

17: Lavender `#cfc4e6`, Mist Blue `#b7c9dd`, Sage `#aebfae`. Air: Sky Blue
`#bfd4e6`, Light Gold `#e6d9c0`. Pro: Cosmic Orange `#c96b34`, Deep Blue
`#2b3a55`, Silver `#dfe0e2`.

## This model

### iPhone 17

The baseline model: two-lens vertical pill, Lavender colorway, 402×874 virtual
screen.

<MockupExplorer
  component="IPhoneMockup"
  variant="17"
  props={{ color: 'lavender', rotation: [0, -0.3, 0] }}
/>

## Mockup-able areas

Every live surface filled with chroma green, labeled with the slot that
feeds it - the fastest way to see exactly where your content lands and
what stays 3D hardware.

<MockupExplorer component="IPhoneMockup" variant="17" screen="chroma" />



<DeviceDisclaimer brands="Apple" />
